

(function(window, document, $) {
    $(window).on('load', function() {
        // selectors

        // Inicializando o Módulo Header
        $.sigarq.routes = {
            getCurrent: () => {
                return true;
            },
        };
    });
})(window, document, jQuery);